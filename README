# pyslickdeals

A way to access [slickdeals](https://slickdeals.net) from the command line. Tested on a Mac.

### Installing
- Install `emojify` with  `brew install emojify`
- Install python3
- Clone the repo.
- Add `slickdeals` to your PATH.